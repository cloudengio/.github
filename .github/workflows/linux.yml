name: linux-setup

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  chrome:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/chrome

  mkcert:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/go
      - uses: ./.github/actions/mkcert

  pebble:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/go
      - uses: ./.github/actions/pebble
