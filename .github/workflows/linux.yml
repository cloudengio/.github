name: linux-setup

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  go:
    uses: ./.github/workflows/go.yml
    with:
      os-version: ubuntu-latest

  chrome:
    uses: ./.github/workflows/chrome.yml
    with:
      os-version: ubuntu-latest

  mkcert:
    needs: go
    uses: ./.github/workflows/mkcert.yml
    with:
      os-version: ubuntu-latest

  pebble:
    needs: go
    uses: ./.github/workflows/pebble.yml
    with:
      os-version: ubuntu-latest
